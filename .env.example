#############################################################################
# WordPress Docker Environment Variables
#
# Copy this file to .env and update the values for your environment.
# DO NOT commit .env to version control as it contains sensitive data.
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your values
#   docker-compose up -d
#############################################################################

#############################################################################
# WordPress & MySQL Database Configuration
#
# These variables configure both the WordPress container's database connection
# and the MySQL container's database/user creation. They are shared between
# both services to ensure consistency.
#############################################################################

# Database user - used by WordPress to connect and created in MySQL
WORDPRESS_DB_USER=wordpressuser

# Database password - used by WordPress to connect and set in MySQL
# IMPORTANT: Change this to a strong password in production!
WORDPRESS_DB_PASSWORD=examplepass

# Database name - used by WordPress and created in MySQL
WORDPRESS_DB_NAME=wordpress

#############################################################################
# MySQL Root Configuration
#
# Root password for MySQL administration (separate from WordPress user).
#############################################################################

# Root password for MySQL administration
# IMPORTANT: Change this to a strong password in production!
MYSQL_ROOT_PASSWORD=password

#############################################################################
# Nginx Proxy Manager Configuration
#
# These variables configure the Nginx Proxy Manager container.
#############################################################################

# Timezone for the proxy manager (used for logs and certificates)
# See: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=America/Chicago
