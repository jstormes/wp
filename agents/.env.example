# Server Configuration
PORT=8001
BASE_URL=http://localhost:8001
NODE_ENV=development

# Agent Configuration
AGENT_CONFIG_PATH=./agents

# Google AI (Gemini) API Key
# Uses GOOGLE_GENERATIVE_AI_API_KEY from environment (set in ~/.bashrc)
# Uncomment and set below only if you want to override the environment variable
# GOOGLE_GENERATIVE_AI_API_KEY=your-google-api-key-here

# Optional: Default model settings
DEFAULT_MODEL=gemini-2.0-flash
DEFAULT_TEMPERATURE=0.7
DEFAULT_MAX_TOKENS=4096

# RAG Configuration
# Embedding model for RAG queries (must match the model used to embed documents)
RAG_EMBEDDING_MODEL=text-embedding-004

# Pinecone (https://www.pinecone.io/)
# PINECONE_API_KEY=your-pinecone-api-key

# Chroma (https://www.trychroma.com/)
# CHROMA_URL=http://localhost:8000
# CHROMA_API_KEY=optional-api-key

# pgvector (PostgreSQL with vector extension)
# PGVECTOR_CONNECTION_STRING=postgresql://user:pass@localhost:5432/vectors
# PGVECTOR_API_URL=http://localhost:3000  # If using REST API wrapper
